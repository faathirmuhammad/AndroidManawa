<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.manawa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Register"></activity>
        <activity android:name=".DetailPeternak" />
        <activity android:name=".Peternak" />
        <activity android:name=".Akun" />
        <activity android:name=".DetailTernakSaya" />
        <activity android:name=".TernakSaya" />
        <activity android:name=".Konfirmasi" />
        <activity android:name=".Pembayaran" />
        <activity android:name=".DetailPemesanan" />
        <activity android:name=".DetailTernak" />
        <activity android:name=".PilihTernak" />
        <activity android:name=".PilihHewan" />
        <activity android:name=".Home" />
        <activity
            android:name=".Dashboard"
            android:label="@string/title_activity_dashboard"
            android:theme="@style/Dashboard" />
        <activity android:name=".Login" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>